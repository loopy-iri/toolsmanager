# سیاست امنیت

## گزارش آسیب‌پذیری

آسیب‌پذیری را همراه نسخه، مسیر بازتولید و اثر احتمالی به مسئول فنی سامانه گزارش کنید. جزئیات امنیتی را پیش از اصلاح در issue عمومی قرار ندهید.

## کنترل‌های فعال

- احراز هویت با نشست سمت سرور و کوکی `HttpOnly`، `SameSite=Strict` و در production با `Secure`
- هش رمز عبور با `scrypt` و salt تصادفی؛ حداقل طول رمز جدید ۱۲ نویسه
- قفل ۱۵ دقیقه‌ای حساب پس از پنج ورود ناموفق و محدودسازی تکمیلی بر اساس IP
- اخذ هویت اقدام‌کننده از نشست، نه بدنه درخواست؛ کنترل نقش برای تایید، تحویل، بازگشت و سرویس
- کنترل Origin روی درخواست‌های تغییردهنده، محدودیت ۳۲ کیلوبایتی JSON و هدرهای CSP/HSTS
- تراکنش `BEGIN IMMEDIATE` برای جلوگیری از رزرو یا تحویل هم‌زمان ناسازگار

## مسئولیت استقرار

TLS باید در reverse proxy خاتمه یابد و `APP_ORIGIN` دقیقاً برابر مبدأ عمومی باشد. فایل `.env` و volume پایگاه‌داده محرمانه‌اند و نباید وارد Git شوند. رمز اولیه باید تصادفی، حداقل ۱۲ نویسه و پس از نخستین ورود توسط هر کاربر تعویض شود. دسترسی میزبان به پورت برنامه فقط از reverse proxy مجاز باشد.

نسخه فعلی MFA، بازیابی خودکار رمز، SSO و مدیریت کاربران در UI ندارد. در سازمانی با الزام هویت مرکزی، پیش از گسترش استفاده باید OIDC/SSO افزوده شود.
